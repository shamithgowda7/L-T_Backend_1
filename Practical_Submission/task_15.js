// Task 15: Hospital Patient System — Apply a Math Method

const ages = [18, 22, 35, 41];
const roundedAge = Math.round(21.6);
const highestAge = Math.max(...ages);

console.log(`Rounded age: ${roundedAge}`);
console.log(`Highest age in the list: ${highestAge}`);
